@startuml piprocess
!include https://raw.githubusercontent.com/doubleSlashde/umltheme/main/puml-theme-doubleslash-activity.puml

skinparam padding 3

'skinparam ConditionStyle normal

|Planung|

   start 

   :doubleSlash Angebot über
   verfügbare Kapa ab;

   :Verfügbare Kapa wird in 
    StoryPoints umgerechnet 
    (1PT = 2SP);

|Entwicklung|


   :Start der Entwicklung;

   if (<size:16><color:red>Effort Changing Event</size>) then (Ja)


   if (Change bewerten) then (Großer Change)
      if() then (Mehraufwand)
       if()
      :StoryPoints erhöhen;
      else
        :Neue User Stories\nanlegen; 
      endif 
      else (Minderaufwand)
      :StoryPoints verringern;
      endif
   else (Kleiner Change)
      :Stories werden mit
      dem vereinbarten
      Aufwand erledigt.;
   endif
   endif

   :Ende der Entwicklung;


|Abrechnung|
   note left:
   :Erledigte StoryPoints
   zusammenzählen;
   :Spill over berechnen;

end


@enduml